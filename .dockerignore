# Segredos e ambiente local — NUNCA na imagem
.env
.env.*
!.env.example

# Virtualenvs (binários de host; quebram/incham a imagem Linux)
.venv/
venv/
env/

# Histórico e metadados de repositório
.git/
.github/
.gitignore
.gitattributes

# Artefatos de agente/IA e config local de ferramenta
.claude/
*.local.md

# Banco de dev e mídia de runtime (vêm de volumes, não da imagem)
*.sqlite3
db.sqlite3*
media/

# Dumps do PostgreSQL. Cinto e suspensório: o kellysys-deploy já grava em
# /var/backups/kellysys, fora do build context — isto protege instalações que ainda
# tenham o diretório antigo em /opt/kelly_sys/backups. Sem isto, o `COPY . .` do
# Dockerfile embute 30 dias de dumps em cada imagem.
backups/

# Material de chave. Mesma armadilha dos dumps: o `COPY . .` do Dockerfile levaria uma
# chave privada para dentro de uma camada da imagem. O deploy guarda a chave SSH em
# /opt/kelly_sys, que E o build context.
*.pem
*.key
*_ed25519
*_ed25519.pub
*_rsa
*_rsa.pub
id_rsa*
id_ed25519*

# Estáticos compilados (regenerados via collectstatic no build)
staticfiles/

# Caches e bytecode
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage
.coverage.*

# Documentação (não necessária em runtime)
docs/

# Binário/config do Tailwind standalone (usado só para gerar o CSS, não em runtime)
tailwind/bin/
tools/tailwind/bin/

# Sistema operacional / editores
.DS_Store
Thumbs.db
.idea/
.vscode/
